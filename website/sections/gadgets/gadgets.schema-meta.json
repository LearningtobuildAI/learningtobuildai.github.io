{
  "schemaUpdated": "2026-08-25",
  "updatedFor": "canonical rulebook v1.4.0 summary fields + PROMPT 1 §3.2/§4.1/§4.2",
  "fieldProvenance": {
    "whatItIs": "the tile's existing `what` text, re-labelled — not rewritten",
    "whatItDoes": "the tile's existing `use` text, re-labelled — not rewritten",
    "howItWorks": "the tile's existing `how` text, re-labelled — not rewritten",
    "materialsRequired": "never recorded for these tiles — set to \"Data not available / not verified.\" per rulebook, NOT guessed",
    "processToMake": "never recorded for these tiles — set to \"Data not available / not verified.\" per rulebook, NOT guessed",
    "energyNeeded": "never recorded for these tiles — set to \"Data not available / not verified.\" per rulebook, NOT guessed",
    "monthAdded": "derived from this file's last-write time (2026-08-05) — these tiles carry no creation date",
    "verifiedPrice": "null — no price was ever recorded, and PROMPT 1 forbids fabricating one",
    "commissionRate": "null — no affiliate account or associate tag is configured anywhere in this repo",
    "affiliateLinks": "amazon-au links only; kickstarter/crowdsupply earn nothing per the page disclosure",
    "commissionLinks": "same set as affiliateLinks (PROMPT 1 §4.2 naming)",
    "shortVideoExplainer": "null placeholder — PROMPT 1 §4.1 says add the field and routing only, generate no videos"
  },
  "openGaps": [
    "materialsRequired / processToMake / energyNeeded are unfilled on all 18 tiles — they need real sourcing, not generation",
    "verifiedPrice unfilled on all 18 — needs a real price feed",
    "commissionRate unfilled — needs an affiliate account first",
    "PROMPT 1 §3.1 asks for NEW tiles (AI PCs, GPUs, wearables, smart home, Kickstarter) — none added; real specs and prices must be sourced",
    "legacy `cost` and `eff` fields still hold the \"—\" placeholder on all 18 tiles"
  ]
}
